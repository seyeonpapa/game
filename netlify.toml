[build]
  publish = "."
  functions = "netlify/functions"

# /api/search → netlify function으로 라우팅
[[redirects]]
  from = "/api/search"
  to = "/.netlify/functions/search"
  status = 200

# Node.js 18+ 환경
[build.environment]
  NODE_VERSION = "20"
